[phases.setup]
nixPkgs = ["python311", "gcc"]

[phases.install]
cmds = ["python -m venv /opt/venv && /opt/venv/bin/pip install -r requirements.txt"]

[phases.build]
cmds = ["echo 'Building Spartan Engine...'"]

[start]
cmd = "/opt/venv/bin/python spartan_agent.py"
